apiVersion: v1
kind: Service
metadata:
  name: {{ include "jyoti-chart.fullname" . }}
  labels:
    {{- include "jyoti-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jyoti-chart.selectorLabels" . | nindent 4 }}
