apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upf-chart.fullname" . }}-test-connection"
  labels:
    {{- include "upf-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upf-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
